# CacheOrchestrator.FusionCache

Registers ZiggyCreatures FusionCache as the <c>IDataCacheProvider</c> for CacheOrchestrator.

Use with **CacheOrchestrator.AspNetCore** (or the meta package **CacheOrchestrator**) for full HTTP + domain orchestration.
