<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <!-- Operator host: net10.0 only (libraries remain multi-target; Admin talks HTTP to any instance TFM). -->
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <LangVersion>latest</LangVersion>
    <RootNamespace>CacheOrchestrator.AdminConsole</RootNamespace>
    <AssemblyName>CacheOrchestrator.AdminConsole</AssemblyName>
    <Description>CacheOrchestrator Admin Console multi-instance fan-out UI over the Admin API on each application instance.</Description>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.OpenApi" Version="10.0.0" />
    <PackageReference Include="Microsoft.OpenApi" Version="2.7.5" />
    <PackageReference Include="Scalar.AspNetCore" Version="2.4.18" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\CacheOrchestrator\CacheOrchestrator.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- Ship rule packs + operator handbook with publish/output -->
    <Content Update="hints\**\*" CopyToOutputDirectory="PreserveNewest" CopyToPublishDirectory="PreserveNewest" />
    <!-- Operator data tree (custom rules mount point + disabled state path in Production) -->
    <Content Update="data\**\*" CopyToOutputDirectory="PreserveNewest" CopyToPublishDirectory="PreserveNewest" />
    <Content Update="README.md" CopyToOutputDirectory="PreserveNewest" CopyToPublishDirectory="PreserveNewest" />
  </ItemGroup>

</Project>
