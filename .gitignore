bin/
obj/
.vs/
*.user
*.suo
TestResults/
artifacts/
appsettings.*.local.json
.env

# Admin App: operator runtime toggles for hint rules (local machine / container volume)
**/hints/disabled.local.json
**/data/disabled.local.json

# Local machine outputs (traces, bench artifacts, scratch)
_local/
nupkg/
*.nupkg
*.snupkg
samples/**/smoke.run.log

# Playground local SQLite (shared path in labs 03–05 is under the Docker volume)
samples/**/Data/*.db
samples/**/Data/*.db-*